services:
  mqtt-dashboard:
    image: nginx:alpine
    container_name: mqtt-dashboard
    ports:
      - "8080:80"   # Accederás en http://IP_PC:8080
    volumes:
      - ./html:/usr/share/nginx/html:ro
    restart: unless-stopped
